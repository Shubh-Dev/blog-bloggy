<% @users.each do |user| %>
<div class="users-container">
    <div class="main-image-container">
        <img src="<%= user.photo %>" alt="user">
    </div>

    <div class="user-details-container">
        <%= link_to user.name, user_path(user) %>
        <h5><%= user.posts_counter || 0 %></h5>
    </div>
<div>

<% end %>